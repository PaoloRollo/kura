import { ClaimHandle } from "@/components/claim-handle";

/** Claim handle (D0ZWe): pick `<label>.kura.eth` once, or skip for now. */
export default function OnboardingPage() {
  return <ClaimHandle />;
}
